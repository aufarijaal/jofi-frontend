import react from 'react';
import { Modal } from '@faceless-ui/modal'

export const MyModal = () => {
  return (
    <Modal slug="my-modal">
      {/* {(modalContext) => {
        return (
          ...
        )
      }} */}
    </Modal>
  )
};
